# Stage 07 Progress

## Status

- Document status: active progress log
- Current Progress: 2026-07-12 Package 1/2 has an implemented, verified vertical path: approved Mini App bootstrap, authorization-filtered Workspace Home, responsive App Shell and workspace switching; permission-filtered Base/table/view canvas including authorized in-Base table switching; read-only Grid/Kanban/Calendar/Form presentation; Record Detail; version-aware scalar direct edits; a server-filtered scalar record-create drawer; bounded P3 atomic Base/Table Builder; bounded F1 Independent Field Builder; and bounded F2 relation/lookup Builder and direct relation editing. P3/F1 have disposable-local PostgreSQL migration and rollback/replay/concurrency/default-view evidence; F1 direct-edit, duplicate-name feedback and pending-dialog lock browser cases are complete at all four required widths. F2 is `implemented-local` only: not Telegram, staging, production or Stage07 acceptance. V1-1 through V1-15 are `partial-local`: real FastAPI + disposable PostgreSQL Browser evidence now covers owner/editor/viewer, Base/Table/Field safe intersection, hidden-field omission, numeric lookup display, one owner Record Detail relation selection/versioned save/authoritative reread, and no page errors. The selected complete existing-contract template/import package is now documented as `specified-awaiting-review`; no implementation or schema/API/permission change has begun. Real stale-version, numeric-filter mutation and all type-invalid paths remain unaccepted. The durable typed/canonical/ACL/query/HTTP/PostgreSQL/transport/panel foundations, Base-list/direct-presentation disclosure correction and `409`/`422` recovery remain enforced. Fresh local Mini App evidence is `24` files / `114` tests plus production build; pre-existing backend focused/full evidence remains `24 passed`, `11 passed` and `512 passed, 17 skipped`. Governance, Bot surface, draft confirmation and final Stage07 acceptance remain incomplete.

## Progress Log

### 2026-07-12: Selected Complete Existing-Contract Template/Import Package — Specification

- User selected Package A: templates shelf/install, Base-to-draft-template save, CSV/XLSX file intake, persistent server preview, scalar mapping and explicit import commit. The package deliberately reuses existing Stage06 routes, authorization, idempotency, audit and PostgreSQL persistence; no schema, API, permission, capability, storage or dependency change is proposed.
- Detailed design, BDD/acceptance, SDD, functional work-surface and complex-feature index now define every state, safe payload, mapping limit, failure rule, authoritative reread and out-of-scope boundary. The documents are `specified-awaiting-review`; no product code, migration, test fixture or external write was created.
- The corresponding four-task implementation plan is written for inline execution and awaits user plan approval. It keeps the package coherent: safe transport/protected state, template management, full import lifecycle, then evidence/cleanup—without a new architecture decision.

### 2026-07-12: V1-15 Real Record Detail Relation-Edit Main Path

- After Browser restart, a single synthetic owner path ran through the built Mini App, real local FastAPI and explicitly disposable PostgreSQL. The owner opened `Editable task`, entered Record Detail edit mode, saw `Editable account` only as the safe relation-candidate label, selected it and saved through the existing versioned PATCH.
- The authoritative result moved Detail from version `1` to `2`; Detail and Canvas both rendered `Editable account`, and the server-produced numeric `sum` lookup rendered `7`. The page-level error scan was `[]` (an external Browser telemetry timeout is not an application console error). This intentionally does not claim stale/type-invalid or broad manual-matrix acceptance.
- Temporary seed/proxy/logs and local processes were removed; ports `8002`/`5173` were closed and the migration smoke reset the disposable database at Alembic head `20260711_0022`.

### 2026-07-12: V1-14 Real Backend Browser Partial Acceptance And Viewer Fail-Closed Repair

- A local disposable PostgreSQL migration smoke reached Alembic head `20260711_0022`, then received only synthetic V1 acceptance data. The built Mini App ran through temporary same-origin role routing to real local FastAPI. Owner saw member controls, numeric lookup sort eligibility and the restricted group list; editor could save but saw no member controls; viewer received permitted title/relation/lookup projection with the hidden `Internal` field absent.
- The first real Browser pass found that viewer still saw an enabled `新建记录` entry. The existing backend `viewer` action map already denies `record.create`; the defect was only Canvas visibility. The Mini App now passes an explicit create-entry boolean derived from the existing server-derived role allowlist (`owner/admin/builder/operator`); viewer and unknown roles are hidden fail-closed. Focused `3 files / 18 tests`, full Mini App `24 files / 114 tests` and `npm.cmd run build` passed. A second real Browser pass confirmed no viewer create/configure entry and allowed data remained visible. Both Browser passes had page error count `0`.
- Temporary FastAPI/proxy/seed scripts and logs were deleted, Browser sessions finalized, ports `8001`/`4176` stopped, and the migration smoke was rerun to remove synthetic data. This improves real-backend evidence but deliberately leaves the full F2 Detail edit/stale/version/type-invalid Browser matrix as `partial-local`.

### 2026-07-12: V1-13 Role And Negative Browser Follow-up

- A second safe-response fixture ran the actual built client as owner, editor and viewer. Owner alone had member administration; editor saved a presentation but had no member controls; viewer saw the restricted tab/record but no create/configure controls. The Builder disabled a 13th filter and fourth sort, exposed only `Status` as a group option, and reused F2 to show a permitted relation label without the opaque ID.
- An editor stale-save `409` displayed only the fixed conflict text and reread `Editor saved view`; raw fixture detail was absent. Page console `error`/`warn` was `[]`. Browser bridge screenshot capture timed out, so no screenshot evidence is claimed; DOM click/fill/select and snapshots are the evidence.
- Fixture process, logs and script were deleted; port `4175` had no listener. This upgrades V1-A03 to `implemented-local`; A02/A05/A07/A08/A10 remain `partial-local` for the explicitly listed real-backend/type/F2 coverage gaps.

### 2026-07-12: V1-12 Browser Evidence And Final Local Reconciliation

- A disposable safe-response fixture served the built Mini App and was exercised in the in-app Browser at 1440, 1280, 430 and 390 widths. The observed flows were private Grid creation plus safe server-authored filter/sort/group summary and owner editor grant; private Kanban grouping; private Calendar plus mobile access sheet; private Form plus focus return to `新建视图`; and fixture `409` canonical reread with raw detail absent. Final console scan was `[]`. The fixture, logs, process and browser session were deleted/finalized; port `4174` had no listener afterwards.
- This is deliberately partial Browser evidence. Owner/editor/viewer identity switching, invalid thirteenth-filter/operator/relation-or-lookup-group/stale-version Browser flows, F2 revalidation, real-backend network inspection and Escape/live-region coverage were not re-run in that closed session. The V1 BDD matrix therefore retains the affected requirements as `partial-local`; neither V1 nor Stage07 is accepted.
- Fresh reconciliation: focused backend `24 passed`; authorized real local PostgreSQL `11 passed`; full backend `512 passed, 17 skipped` (only historical Stage02 online smoke without `STAGE02_ONLINE_DATABASE_URL`); Mini App `24 files / 114 tests`; production build passed; Alembic shows one head `20260711_0022`. Detailed evidence: `evidence/stage07-v1-view-builder-ui.md` and `evidence/stage07-v1-view-builder-verification.md`.

### 2026-07-12: V1-11 Conflict, No-Disclosure And 390px Recovery

- Red application tests first showed that a stale presentation save retained its local draft after `409`, a stale member replacement retained owner grant controls, and closing the 390px Builder dropped focus to the document body. The green path reuses exact protected query cleanup and canonical server reads: presentation conflict rereads Base list/Builder/records; member conflict rereads Base list/Builder only because grants do not change the owner's current safe record projection. Neither path retries a mutation.
- The client continues to map only fixed `view_*` error codes. `422` retains safe local draft values; unknown/network errors remain generic and no server `message`/detail is rendered. Owner/editor/viewer authority is still server-derived; no raw config, policy, hidden field or member metadata enters the recovery state.
- Mobile CSS no longer hides the V1 configure/create entry points. The Builder has horizontal-overflow containment, scroll-safe content, 44px mobile controls and sticky actions; close returns focus to the triggering Canvas control. This is component/application evidence at 390px, not a real-browser visual acceptance claim.
- Fresh V1-11 verification: focused error/responsive suite `2 files / 4 tests`, full Mini App `24 files / 114 tests`, `npm.cmd run build` passed. Backend/PostgreSQL source evidence is unchanged from V1-10. Browser Task 12 remains required.

### 2026-07-12: V1-10 Authoritative Canvas Lifecycle And V1 List ACL Closure

- The Mini App now opens a V1 Builder only from an already safe V1 list marker, creates a private view through the typed initializer, and switches Canvas only after a fresh Base list contains the exact persisted receipt ID. Creation and versioned presentation saves clear the exact protected Builder/context/record keys, then reread Base list, canonical Builder response and current record window. Member replacement clears and rereads only the Base list and Builder because it cannot alter the owner's current safe record projection. No synthetic tab, client ACL shadow state or client query execution was added.
- BaseCanvas removes the inert filter/sort/group controls. It shows a server-authored filter/sort/group summary and V1 configuration/create entries only behind existing server-derived capability hints. Grid/Kanban/Calendar/Form dispatch consumes the safe server presentation and returned record sequence; test IDs prove all four renderers mount without browser predicate/sort execution. Record Detail and the existing F2 relation editing path remain unchanged.
- During this integration an actual server gap was found: `GET /bases/{base_id}/views` could list private/restricted V1 summaries to an otherwise table-authorized caller, and the generic direct presentation path could bypass V1 access. The existing effective V1 resolver now filters Base-list rows and guards direct V1 presentation reads. A permitted V1 list row adds only `scope`, `caller_access_level` and `is_default`; legacy rows and generic Builder receipts retain their prior shape. No role, endpoint, schema, migration, policy, public sharing or raw-data read model was introduced.
- Fresh verification: V1 real disposable PostgreSQL `11 passed`; full backend `512 passed, 17 skipped` (only historical Stage02 online-smoke skips without `STAGE02_ONLINE_DATABASE_URL`); focused/full Mini App `22` files / `110` tests; `npm.cmd run build` passed. This is local evidence only. Browser acceptance, responsive/error matrix, Telegram, staging and production are not claimed.

### 2026-07-12: V1-9 Safe Builder/Access Panel Layer And Local PostgreSQL Recheck

- Added `ViewBuilderPanel`, `ViewQueryControls` and `ViewAccessPanel` by reusing existing F1/F2 native drawer and controlled-form patterns. They produce only typed V1 commands: private initial creation, versioned presentation save and full owner-only member replacement. Grid exposes bounded field order/visibility, flat `AND` filters, three-sort and one-group controls; the shared controls provide safe type settings for Kanban/Calendar/Form. Select-like filters consume only `filter_values`; relation filters reuse the existing F2 picker through safe `field_id`; no raw configuration, policy, option object, browser permission inference or client query execution was introduced.
- The initial component tests were red because both modules were absent. The green focused/regression command reported `4` files / `13` tests; full Mini App verification reported `21` files / `102` tests and `npm.cmd run build` passed. A TypeScript narrowing issue for Calendar was found by the build and fixed by limiting group-key access to Grid/Kanban.
- The authorized disposable local PostgreSQL migration smoke reached `20260711_0022`; after adding a real Builder-context route test for safe `field_id` and select `filter_values`, the V1 integration/security matrix reported `10 passed`. It is local PostgreSQL evidence only, not an App lifecycle, Browser, Telegram, staging or production claim.
- The panels are deliberately callback-driven in this substage. No toolbar entry, selected-view state, optimistic mutation, fake local view, Browser test, public share, group/delegation, delete/default reassignment, import/template or production work was added. V1-10 owns App/BaseCanvas authoritative rereads and renderer integration.

### 2026-07-11: V1-8 Typed Mini App Transport And Protected Keys

- Added a V1-only type module for the five safe route commands and response shapes. `api.ts` encodes every table/view path segment, sends `Idempotency-Key` only on private initialization, sends version only in PATCH/PUT bodies, and explicitly reconstructs safe context/summary/presentation/member receipts so unexpected response keys such as owner/config/policy cannot enter browser state.
- The V1 key helper always begins with existing verified user/workspace scope and adds context or view/version segments. Fixed Chinese feedback maps only documented `view_*` codes and never uses server `message`; unknown values fall back to one generic sentence. No persistent storage, optimistic mutation, React UI component or cache hydration was introduced.
- Red transport tests first failed for missing V1 methods/error mapper. Green evidence: V1/API/protected-query tests `15 passed`; whole Mini App `19` files / `96` tests passed; `npm.cmd run build` passed. No Browser fixture/run, Builder panel, Canvas integration, Telegram, staging or production work was added.

### 2026-07-11: V1-7 Disposable PostgreSQL Security And Index Decision

- The real PostgreSQL matrix adds default Grid/private-view separation, route-level rollback after post-initialization safe-projection failure, and a viewer hidden-field scan across records, presentation, denied builder and denied context. The matrix passed `9` cases at Alembic `20260711_0022`; the one test setup repair was an explicit `flush` required because this fixture sets `autoflush=False`, not a production behavior change.
- PostgreSQL `18.4` `EXPLAIN (ANALYZE, BUFFERS)` over 128 V1 views/32 grants used existing `ix_stage06_views_table_id` and `uq_view_member_grants_view_user`, with a 0.180 ms execution sample. `ix_views_table_scope_status` and `ix_view_member_grants_user_status` were absent and not justified, so both remain explicitly deferred; no migration was created. The full sanitized evidence and cleanup boundary are retained in `evidence/stage07-v1-view-builder-postgres.md`.
- Fresh final backend regression: `509 passed, 17` historical Stage02 online-smoke skips. This is local disposable PostgreSQL evidence only, not staging, production, Telegram or Mini App acceptance.

### 2026-07-11: V1-6 Safe HTTP Surface And Fixed-Code Errors

- Red API evidence first returned `404` for `GET /tables/{table_id}/view-builder-context`. The green surface exposes exactly five V1 routes: builder context, private initialization, builder read, presentation PATCH and member replacement PUT. `Idempotency-Key` is accepted only on initialization; `expected_version` remains typed body data for PATCH/PUT.
- Context requires active `table.read` plus `view.manage` and returns only table summary, eligible safe fields, caller-accessible safe summaries and active `{ id, label }` candidates. Builder reads require owner/editor (or existing default `view.manage`), suppress grants for editors, and never return raw config/policy/owner/member-role/status/field options. Creation/replay returns `201`/`200` safe receipts; owner-only member replacement normalizes scope; editor presentation mutation succeeds; viewer builder/mutation denial is `403/view_access_denied`; stale PATCH is `409/view_version_conflict`; unknown `scope` fails strict request validation.
- Existing `POST /bases/{base_id}/views` remains untouched and the unapproved `/tables/{table_id}/views` path is `404`. Existing non-V1 record responses still omit `groups`. Fresh targeted API/legacy regression: `9 passed`; complete backend: `505 passed, 17` historical Stage02 online-smoke skips. No Mini App transport, new physical index, Browser run, Telegram, staging or production work was added.

### 2026-07-11: V1-5 Server Query Execution Before Cursor Pagination

- Red tests first showed that the legacy paginator reordered rows by record ID, that a V1 Grid was returning raw record order, and that it emitted no grouping result. The green V1-only path resolves effective view/table/field authority, obtains the current safe presentation, filters authorised projected rows with the canonical flat `AND` conditions, applies one permitted safe group key then up to three configured sort rules plus a record-ID tie-breaker, and passes that exact order to cursor pagination. Existing non-V1 view pagination retains its former ordering path.
- The current generic record storage requires a bounded service interpreter over already-authorised record projections; it accepts no query text, raw SQL, arbitrary operator, client filter or client sort. It uses the previously canonicalized F2 numeric lookup/relation eligibility only, never evaluates hidden fields, and exposes no raw sort/filter values through the browser contract.
- For a configured group key, the safe response has `groups: [{ value, record_ids }]` for that page only. The value comes from a currently readable status/single-select/user field, the IDs belong only to already returned safe records, and no raw config, policy, hidden field or member state is emitted. The browser is still not wired to consume this field.
- Fresh focused evidence: `test_stage07_view_builder_query_execution.py`, existing pagination/API tests and the disposable PostgreSQL suite reported `8 passed`; the PostgreSQL case proves `score >= 3`, active-before-closed grouping and descending score order produce `alpha` then `charlie` across a cursor boundary. The legacy exact-response/API regression proves non-V1 views do not receive a `groups` field. Full backend regression: `503 passed, 17` historical Stage02 online-smoke skips. No V1-specific HTTP route, client capability, Mini App state, Browser run or physical index was added.

### 2026-07-11: V1-4 Versioned Access, Idempotent Private Creation And Atomic Grants

- Red tests first proved private initialization, owner/editor/viewer separation, version conflict, invalid role bypass, inactive view rejection and one unified effective-access resolver were absent. The green service path uses Stage06's idempotency record/fingerprint pattern, creates owner-private view/version `1`, writes sanitized audit, resolves active membership plus table authority plus view role, and makes grant replacement owner-only/atomic with scope normalization.
- Editor can update presentation but cannot grant; viewer cannot mutate; default view cannot receive grants; owner/self, duplicate, inactive and non-`editor`/`viewer` recipients fail closed. Every presentation/grant mutation row-locks through the UoW and requires exact `expected_version`.
- Fresh disposable local PostgreSQL evidence in `test_stage07_view_builder_postgres.py`: `4 passed` covering migration columns/unique grant constraint, one private row/one idempotency replay, physical unique-grant rejection and concurrent member replacement (`updated` plus `view_version_conflict`). Full backend regression: `499 passed, 17` historical Stage02 online-smoke skips. No HTTP endpoint, record query execution, client capability, Mini App state or browser evidence was added.

### 2026-07-11: V1-3 Server-Owned Canonical Presentation And Safe Projection

- Red tests first proved canonical V1 validation and safe V1 projection were absent. The green service path uses readable active fields only, makes a JSONB-ready canonical presentation, enforces flat `and`, twelve-filter/three-sort bounds, unique stable sort keys, one eligible group, Calendar date, Form writable fields and safe choice values.
- F2 integration remains bounded: relation filters require an existing safe candidate in the relation target table; numeric lookup (`count`, `count_distinct`, `sum`, `average`, `min`, `max`) may filter/sort, while `values` lookup and every relation/lookup group remain invalid. Safe reread removes hidden/invalid configured fields, filters, sort metadata, group/date/form keys and never returns raw `config`.
- Fresh evidence: focused canonicalization plus existing Stage06 API/F2 regressions reported `43 passed`; full backend reported `490 passed, 17 historical Stage02 online-smoke skips`. This task does not write a view, authorize a caller, create an endpoint, query records, audit a mutation or change Mini App behavior.

### 2026-07-11: V1-2 Strict Typed View Command And Safe Read Schemas

- Red tests first proved all V1 commands and safe views were absent. The green schema boundary adds `StrictViewBuilderModel` (`extra="forbid"`), a discriminated Grid/Kanban/Calendar/Form presentation union, explicit private initialization, versioned presentation/member mutation commands, `editor`/`viewer` grants, and typed safe view summary/presentation/member/field/builder models.
- The schema rejects raw `config`, `permission_policy`, owner identity, scope/default/state mutation fields, more than twelve filters, more than three sorts, non-`and` conjunctions, invalid member access levels and more than one hundred grant recipients. It does not decide field eligibility, caller authority, idempotency, canonicalization or persistence; those remain service tasks.
- Fresh evidence: the dedicated schema/API regression command reported `11 passed`, and the complete backend suite reported `485 passed, 17 historical Stage02 online-smoke skips`. No route, service behavior, audit, PostgreSQL fixture, Mini App component or browser state was introduced.

### 2026-07-11: V1-1 Durable Saved-View Persistence Foundation

- Red tests first proved that V1 ownership columns, member-grant model, migration and UoW operations did not exist. The green path adds `PlatformView.owner_user_id`, `scope`, `version`, `ViewMemberGrant`, migration `20260711_0022_stage07_saved_view_builder`, and in-memory/SQLAlchemy UoW methods for view lock, grant replacement/listing and data-level accessible-view lookup.
- The migration is additive after `20260710_0021`; existing views receive database defaults `scope=system_default` and `version=1`, the existing default-Grid partial index is untouched, and `uq_view_member_grants_view_user` protects one recipient row per view. Alembic reports one head: `20260711_0022`.
- No V1 HTTP endpoint, idempotent initializer, ACL decision service, canonical query/config validation, audit, optional non-unique access/list index, Mini App state or Browser fixture is added. `ix_view_member_grants_user_status` and `ix_views_table_scope_status` remain explicitly deferred until Task 7 records the SDD-required PostgreSQL `EXPLAIN` evidence.
- Fresh focused verification: `python -m pytest -q tests/unit/test_stage07_view_builder_migration.py tests/unit/test_stage06_builder_default_view_migration.py tests/unit/test_stage06_platform_core.py` reported `17 passed`; the pre-change full backend baseline was `477 passed, 17 historical Stage02 skips` and Mini App baseline was `18 files / 93 tests` plus production build.

### 2026-07-11: V1 Comprehensive Saved View Builder Design And TDD Plan Approved

- The user asked to stop fragmenting the next stage into repeated micro-confirmations and instead document the complete design for one review. The V1 package therefore covers all saved view types (`grid`, `kanban`, `calendar`, `form`) rather than treating Grid as the endpoint of scope. It records selected constraints: newly created views are private, owner may explicitly grant active members `editor`/`viewer`, the system default remains the only shared default Grid, every ACL intersects with underlying platform authority, Grid uses server-only flat `AND` filters/up to three sorts/one group, and F2 relation/numeric lookup participation stays bounded.
- Added a complete design specification, detailed BDD/acceptance matrix, SDD, module work-surface document and separate complex-feature/index inventory. They reject reuse of the legacy raw `POST /bases/{base_id}/views` config/policy contract, reuse P3/F1/F2 idempotency/audit/protected-query patterns, propose strict typed safe commands/read models, durable owner/scope/version and member grants, and define migration/query evidence before any implementation.
- The user approved this comprehensive written design and then approved its implementation. The linked TDD plan enumerates persistence/migration, strict schemas, server-owned canonicalization/query execution, ACL/versioning, route, PostgreSQL, Mini App, four-width Browser and final evidence tasks. The later V1-1/2/3 entries record the first runtime changes; all end-to-end V1 acceptance rows remain unaccepted until their owning tasks complete.

### 2026-07-11: F2 Real PostgreSQL Closure, Same-Record Picker Guard And Bounded Local Acceptance

- The authorised disposable `STAGE06_LOCAL_DATABASE_URL` was reset/migrated through `scripts/stage06_local_postgres_migration_smoke.py` before the dedicated F2 command passed `6` cases. The new projection case creates three readable target records (`2`, `2`, `5`) and one source relation, then reads one safe view record containing every approved aggregation: `values=[2,2,5]`, `count=3`, `count_distinct=2`, `sum=9`, `average=3`, `min=2`, `max=5`. The same suite also proves relation rollback, concurrent initializer ordering, same-key replay, lookup rollback and the reusable record/relation/target dependency guards. It ran only against the documented disposable local target.
- While reconciling the direct Detail path against the F2 work-surface rule, a real UI gap was found: a server candidate page containing the current same-table record could render it in the Picker. A new frontend test was red, then `RelationPicker` received one narrow `excludedCandidateId` filter from `RecordDetail`. The server still performs the authoritative `relation_self_reference` recheck; this UI filter prevents presenting an invalid self choice. No API, schema, permission, migration, dependency or capability changed.
- Final local Browser QA used a synthetic fixture at `1440x900` and `390x844`. At 1440, Grid and Detail safely displayed the seven values `["alpha","beta"]`, `73`, `59`, `107`, `13.5`, `2`, `97` with no opaque target ID. In relation edit mode at both widths, `Current record` candidate count was `0`, `Other record` candidate count was `1`, selected chip count was `1`; removing and reselecting the permitted target completed without local UI error. The in-app Browser's long-panel pointer translation could not physically activate the bottom submit control, so this entry does not claim a new Browser PATCH-success observation; the existing primary fixture and frontend/application PATCH regressions remain the direct-save evidence. The final local-origin console error/warn scan was `[]`.
- Fresh final verification: `npm.cmd test -- --run` reported `18` files / `93` tests, `npm.cmd run build` passed, `python -m pytest -q tests/integration/test_stage07_relation_lookup_postgres.py` reported `6 passed`, and full backend `python -m pytest -q` reported `477 passed, 17 skipped`. The skips are only historical Stage02 online smoke tests lacking `STAGE02_ONLINE_DATABASE_URL`.
- F2-A01 through F2-A10 are now `implemented-local` in the dedicated BDD matrix and requirement audit. This closes only the approved F2 bounded slice. No reverse/cross-Base relation, delete route/UI/cascade, formula DSL, migration/index, new role/capability, persistent client storage, V1/import/template/Bot/Telegram or production work was added. Temporary fixture/proxy/Vite processes and the fixture source must be removed before commit.

### 2026-07-11: F2 Mobile Negative-State Evidence (Partial)

- The disposable local fixture was exercised in the actual Mini App at `430x844`: after a relation initializer returned `409`, the full-height F2 Builder sheet retained the submitted name but disabled relation/lookup choice, target-table selection, required checkbox and submit action. It showed only the fixed local conflict text, while Cancel remained usable. This is the documented `conflict-locked` state, not a successful field creation.
- At `390x844`, starting the `新建记录` flow mounted the relation candidate path; a fixture `403` returned only the existing global `无工作区访问权限` boundary with `当前身份没有可访问的工作区。`. No candidate label, opaque target ID, target-table detail or server error message was rendered. The local-origin (`127.0.0.1`) Browser console `error`/`warn` scan was `[]`, demonstrating that the Picker rejection is contained instead of becoming an unhandled promise.
- The same disposable fixture then returned a replay-shaped `200` relation receipt at `430x844`. The Builder closed only after the app reread the authorised table schema/presentation/records/create-form models and the new `Replay relation` header was present; it was not an optimistic receipt render. At `390x844`, a `lookup_target_incompatible` `422` retained the lookup Builder and showed exactly `目标字段与聚合方式不兼容。`; the fixture-only detail `fixture secret backend detail must never render` was absent. The final local-origin console `error`/`warn` scan was `[]`.
- This closes the documented mobile replay-after-reread and allowlisted-invalid gaps only. The authorised disposable F2 PostgreSQL matrix and final requirement-by-requirement audit remain open; no runtime/API/schema/permission change was made in this evidence-only substage.

### 2026-07-11: F2 Picker Rejection Containment And Desktop Negative-State Evidence

- A browser-observed `403` candidate request exposed an unhandled promise from `RelationPicker`: App correctly cleared the protected scope and rendered the generic denied boundary, but `void loadPage()` still wrote a console error. Test-first repair now consumes the rejected Picker promise, clears only an initial/search page failure, and increments the request generation during field replacement/unmount so stale completions cannot write state. It does not change the relation candidate API, authorization, cache-key contract or safe error allowlist.
- An independent review found and blocked a pagination regression in the first repair: a failed cursor page incorrectly cleared the authorised first page. The final branch preserves `candidates`, `nextCursor` and `hasMore` when `cursor !== null`, so `available -> next-page-loading -> available` retains the safe retry entry. The focused relation-picker suite is `3 passed`; its initial-rejection case relies on Vitest's configured unhandled-rejection gate, and the added cursor-failure case directly proves retained candidate and `Load more` state. Independent re-review approved the final branch with no Critical/Important finding.
- A second disposable local fixture exercised the documented desktop negative states without real user or business data: a nested lookup rendered `42` through the safe Canvas projection; a replay receipt (`200`) closed the Builder only after the authorized reread; `409` held the Builder locked; `lookup_target_incompatible` mapped to fixed local text without the server message; and candidate `403` rendered only the existing generic `无工作区访问权限` boundary. The fresh denied-state Browser console error/warn scan returned `[]`.
- Fresh final checks: Mini App `npm.cmd test -- --run` passed `18` files / `92` tests; `npm.cmd run build` and `git diff --check` passed. The full backend `python -m pytest -q` run after aligning the legacy relation-projection assertion passed `463`, skipped `27`; the skipped F2 PostgreSQL cases remain uncounted because `STAGE06_LOCAL_DATABASE_URL` is absent. The temporary fixture was deleted, Vite/fixture processes stopped and ports `4173`/`4174` had no listeners.
- At that checkpoint, the real disposable F2 PostgreSQL matrix, documented mobile negative-state permutations and final requirement-by-requirement F2 acceptance audit remained open. The later mobile-evidence entry closes the replay/allowlisted-invalid gap only. No migration, physical index, dependency, persistent browser storage, capability/role, DELETE path/UI, reverse/cross-Base relation, formula DSL, V1/import/template/Bot/Telegram or production work was added.

### 2026-07-11: F2 Frontend Detail, Safe Rendering And Builder Substage

- Commits `09de7fc`, `a38b912`, `0871dee`, `3a8c2bc`, `9c25172`, `919cdc7` and `b22ee19` complete the bounded Mini App path that was approved for F2: server-scoped candidate pages are reused by record create and direct edit; Detail maps safe `{ id, label }` selections to the existing versioned PATCH ID-array payload; lookup remains read-only; and Grid, Kanban, Calendar and Form render relation labels/chips rather than JSON or opaque IDs.
- The F1 drawer retains its independent-field request contract. Its explicit F2 entry opens a separate `RelationLookupFieldBuilderPanel`: relation submits only `name`, target table ID and `required`; lookup submits only name, source relation field ID, target field ID and one of the seven approved aggregation literals. The browser never displays raw IDs, relation/lookup options, policy or a formula DSL. Target compatibility, graph/depth and permission decisions remain server-owned.
- F2 Builder reuses protected Base/table schema reads in parallel, existing TD001 user/workspace query keys, dedicated relation/lookup initializer methods, exact field/view/form invalidation and authoritative schema reread before receipt-field rendering. It has explicit 401/403/404/unknown fail-closed paths, a retry-only idempotency key, 409 lock, and no optimistic field insertion. Two review-found lifecycle races were fixed test-first: a Detail close/table/view transition clears exact candidate keys, and F2 schema prefetch now enters a non-submitting loading drawer that is synchronously cleared on close, workspace, table or view replacement so an old response cannot restore an obsolete builder.
- Fresh frontend evidence after the final lifecycle repair: `npm.cmd test -- --run` passed `17` files / `87` tests and `npm.cmd run build` passed. Focused and full task reviews passed after the two lifecycle repairs. This checkpoint was later extended by the F2 integration/browser entry below.
- Explicitly not added: a migration, physical database index, package, persistent browser cache, new capability/role, raw configuration read model, relation target traversal, formula DSL, reverse/cross-Base relation, field delete/edit UI, DELETE route, automatic unlink/cascade, V1 views, import/template, Bot, Telegram or production work. Remaining F2 evidence is listed in the Test Plan and traceability audit.

### 2026-07-11: F2 Integration Race Repair And Primary Four-Width Browser Evidence

- Commits `31b8784` and `8260154` add the F2 application evidence required before browser work. A delayed relation-field receipt reread cannot restore an old workspace/field; mock-transport create and Detail direct edit submit only relation ID arrays while Canvas/Detail show safe labels and a server-projected lookup value. A `PATCH 404` now clears only candidate keys belonging to `linked_record` fields that are actual own keys of the active Detail values. The focused negative fixture proves a same-table relation field absent from Detail is retained; no global cache removal was introduced.
- Disposable local Browser QA used a temporary proxy/fixture with no real user or business data. At `1440px`, the UI created a relation field, created a numeric `sum` lookup through the separate builder, rendered `Acme Co` as a chip and `128.5` as a lookup result, loaded the second candidate page, created a relation value and directly edited the record relation. The Detail exposed the lookup as read-only output, not an editable control. At `1280px`, the rendered Detail and chip list remained readable; at `430px`, the F2 Builder occupied the mobile sheet bounds; at `390px`, the Relation Picker occupied the mobile create panel bounds. The Browser-reported content bounds were `414.67px × 900px` for the 430 Builder and `374.67px × 844px` for the 390 Picker. The final page identity was `http://127.0.0.1:4174/` / `工作台`; final console error/warn logs were `[]`.
- Historical checkpoint, superseded by the F2 real-PostgreSQL closure entry above: cleanup was verified; the temporary fixture script was deleted, fixture/Vite process IDs were stopped, and ports `4173`/`4174` had zero listeners. That checkpoint reported `18` files / `90` tests and a dedicated F2 PostgreSQL suite `2 skipped` because `STAGE06_LOCAL_DATABASE_URL` was then absent. It is retained solely to explain chronology, not as current evidence.
- This is primary happy-path browser evidence only, not final F2 acceptance: the Browser matrix still lacks the documented denied/invalid/replay states, nested-lookup browser state and a real disposable PostgreSQL pass. Full backend regression after final F2 reconciliation and the requirement-by-requirement final audit also remain open.

### 2026-07-11: F2 Safe Read/Write, Lookup Evaluation and Guard Substages

- Commits 5690092, ca63786, c5dd5e1 and fe0b44f complete the local in-memory/API-safe slice: server-composed candidate pages, safe relation cells, required/self/target relation validation, safe mutation rereads, seven fixed aggregations, one nested lookup level, whole-field fail-closed lookup omission and reusable incoming-link/field-dependency guards.
- F2 adds no public DELETE route/UI, automatic unlink/cascade, migration, physical database index, client-side join, formula DSL, capability or persistent browser data. The guards are future deletion-path preconditions only.
- Focused F2 plus existing Stage07 field/API unit regression reported 53 passed. PostgreSQL transactional/concurrency proof, Mini App F2 transport/query/UI implementation, full frontend regression, four-width Browser QA, full-backend regression and final traceability audit remain open.

### 2026-07-11: F2 Safe Candidate and Relation-Write Substages

- Commit 5690092 adds server-composed relation candidate pages and server-only relation cells. Candidate records contain only opaque ID/label pairs; labels use readable primary/fallback text fields; raw target values/policies are not returned. Existing view/detail projections no longer expose linked-record ID arrays.
- Commit ca63786 keeps existing create/versioned PATCH as the only relation write protocol and rejects required empty relation values, duplicate IDs, wrong target tables, targets without safe readable labels and same-record self links. Record mutation responses reread the authorised safe projection; create-form includes a linked field only when it is writable and picker-safe.
- Focused F2 plus existing Stage07 field/API unit regression reported 43 passed. This is local unit evidence only. Lookup runtime aggregation/fail-closed hop evaluation, dependency delete guards, PostgreSQL, Mini App Picker/rendering and browser acceptance remain open.

### 2026-07-11: F2 Documentation Reconciliation (Execution Paused)

- The user required Stage07 documentation to meet the prior-stage standard before further F2 implementation: detailed BDD, SDD, per-function module documentation and a separately navigable complex-feature index.
- Added STAGE_07_F2_RELATION_LOOKUP_BDD_AND_ACCEPTANCE.md, STAGE_07_F2_RELATION_LOOKUP_SDD.md, modules/STAGE_07_F2_RELATION_LOOKUP_WORK_SURFACE.md and STAGE_07_F2_RELATION_LOOKUP_COMPLEX_FEATURE_INDEX.md. Together they specify normal, rejection, permission degradation, idempotency, rollback, concurrent, cancellation, cache, relation-write, lookup-read and responsive states; they map each requirement to implementation/test/evidence ownership.
- The complex-feature index explicitly distinguishes logical field/dependency/link/query-key indices from physical PostgreSQL indices. F2 authorises no migration or speculative database index; a future physical index requires measured query evidence, a migration decision and separate user approval.
- Existing F2 design choices are restated rather than broadened: same Base only, no reverse field, ordered multi-select relation, fixed aggregation enum, two lookup levels, full fail-closed lookup omission, existing versioned record writes, no DELETE route/UI/cascade and no new role/capability/dependency/persistent browser storage.
- Local branch commits 37b1e95 (safe transport contract), c3688da (atomic relation initializer) and cac3d57 (bounded lookup initializer) remain narrow implementation evidence only. Lookup initializer unit/API behavior is locally verified, but candidate selection, record write rechecks, evaluator, PostgreSQL and UI evidence remain unfinished and must not be used as F2 acceptance evidence.

### 2026-07-11: F2 Relation/Lookup Design and Plan Complete (No Runtime Code)

- The user selected and approved a bounded F2 design: same-Base ordered multi-select relation (including same-table but not self-record links), read-only lookup, fixed aggregation enum, at most two lookup levels, graph cycle denial, complete fail-closed permission degradation, safe `{ id, label }` relation cells, service-composed candidate paging, versioned existing record writes, required relation semantics, no cascade delete and server-owned active-view append.
- The approved written specification is `docs/superpowers/specs/2026-07-11-stage07-f2-relation-lookup-design.md`. It deliberately reuses the existing Stage06 JSONB/`RecordLink`/authorization/audit and F1/P3 initializer patterns; it does not copy raw Lark API/configuration and introduces no dependency, migration, role/capability or browser persistence decision.
- The implementation plan is `docs/superpowers/plans/2026-07-11-stage07-f2-relation-lookup-implementation.md`. It requires TDD, real disposable PostgreSQL evidence, all four UI widths and TD001 protected-state regression before any F2 local acceptance claim.
- No endpoint, schema, database migration, permission implementation, frontend component or UI behavior changed in this documentation substage. The plan itself records that the current backend has no public record/field delete route; F2 must not add one incidentally.
- The next gate is user review/approval of the detailed plan and a separate explicit authorization to implement its proposed API/read-model/permission-enforcement changes. V1 remains a separate later design discussion.

### 2026-07-11: Direct Record Mutation Safety Repair (TD001 Scope)

- Scope stayed inside the already-approved memory-only protected-query decision. No backend route, schema, authorization rule, dependency, API request/response contract or field type changed; F2 relation/lookup and V1 additional View Builder were not started.
- A review found that a late direct-edit `403` could clear cache without always replacing the active same-workspace view with the generic denied boundary, and that a concurrent completion after `401` could attempt a protected reread. `App.tsx` now invalidates all request generations on identity expiry, makes every direct-edit/conflict continuation observe the session latch, uses TanStack `isCancelledError`, and treats an affected active workspace's `403` as fail-closed even if the record/view generation is stale.
- The exact record plus active first view window are cancelled and removed on record-level `404`, record switch, view/table replacement and detail close. Other record IDs, cursor windows, views and workspaces are retained. This is verified in the query-state unit test; a workspace `403` remains prefix-scoped.
- Test-first evidence includes late-`403` after record switch, `401` racing a successful direct PATCH, close-during-conflict refresh cancellation, 401/403/404 direct-save and conflict branches, workspace switch during delayed PATCH, and exact cache-cleanup scope. Fresh Mini App verification: `14` test files / `67` tests passed; `npm.cmd run build` passed.
- Disposable local UI evidence used only fixture data: Home → 客户管理 → Ada Wong edit → delayed PATCH → 产品协作工作区 switch. After the old PATCH resolved, the UI remained on `需求规划`, `Ada Ltd` was absent, and final console warnings/errors were `[]`. The fixture/proxy/Vite processes and temporary files were removed after the check.
- This closes only the TD001 direct-record mutation safety repair. It is not a real Telegram/backend authorization run, full four-width visual QA, production evidence or Stage07 acceptance.

### 2026-07-11: P3/F1 Real PostgreSQL Proof and Transaction Flush Repair

- An authorised disposable `stage06_smoke` database was reset and migrated with `scripts/stage06_local_postgres_migration_smoke.py`; it reported Alembic `20260710_0021` and the expected platform tables. No development or production database was used.
- The first real P3 run exposed a genuine SQLAlchemy-only defect hidden by the in-memory UoW: the atomic initializer staged a Base/Table and immediately re-read it with `Session.get()` before it had been flushed, producing `base_not_found` and then `table_not_found`. The minimal repair flushes each newly staged Base/Table inside `SqlAlchemyStage06PlatformUnitOfWork`; the existing forced-rollback test was red before the repair, progressed from `base_not_found` to `table_not_found` after the Base-only change, then passed after the Table flush. Two missing imports in the P3 real-database test were also corrected.
- Fresh evidence: the combined command for three F1 and three P3 real PostgreSQL cases passed `6 passed, 2 deselected in 9.19s`; a full backend regression with the same disposable URL passed `440 passed, 17 skipped in 23.10s`. The remaining skips are only historical Stage02 online-PostgreSQL tests without `STAGE02_ONLINE_DATABASE_URL`.
- P3 and F1 are now `implemented-local` bounded substages. This does not implement F2/V1, imports/templates, governance, Bot/draft UI or Telegram identity/deep links, and does not claim staging, production or full Stage07 acceptance.

### 2026-07-11: F1 Duplicate Feedback and Pending Lock Initial Evidence at 1440px

- User-approved error presentation is deliberately narrow: the Mini App transport parses only `422.detail.code = duplicate_field_name`; `FieldBuilderPanel` maps that one allowlisted value to `字段名称已存在，请使用其他名称。`. It ignores `detail.message` and every unknown code, leaving the generic safe error path intact. No request parameter, response resource shape, authorization decision or policy data was added.
- Test-first evidence: the transport and panel tests were first red because `ApiError` retained only the status and the panel chose its generic message. The minimal transport allowlist and local mapping made the focused two-file run green (`10 passed`); the fresh full Mini App suite then reported `13` files / `55` tests passing and the production build passed.
- Disposable 1440px Browser QA used the real fixture transport chain. It retained `客户阶段`, rendered the fixed duplicate feedback without `field_name`, then held a request pending and observed `创建中…`, `关闭` and `取消` disabled while the modal stayed visible. The final console error/warning check was empty. The fixture was deleted and local server stopped after the run.
- This was the initial 1440px observation. The four-width closure is recorded in the newer entry below; delayed workspace replacement remains covered by the already-existing application scope-isolation test. At that checkpoint F1 was `partial-local` solely because its three PostgreSQL proof cases were environment-gated; the later real-database entry supersedes that status.

### 2026-07-11: F1 Four-Width Error and Pending Browser Matrix Closed

- The disposable F1 error fixture was rerun at `1440px`, `1280px`, `430px` and `390px` through the actual Mini App transport chain. Each duplicate-name run retained `客户阶段`, showed exactly `字段名称已存在，请使用其他名称。` and did not render the server-only `field_name` message.
- The companion pending-request run at each width kept the drawer/sheet visible while `创建中…`, `关闭` and `取消` were disabled. This is the correct scope behavior: a background workspace/view switch cannot be forced while the modal safely prevents it; the separate application test proves a delayed old-scope receipt cannot restore a stale field.
- Every final browser console query returned `[]`. The local Vite server, disposable fixture source/import and browser session were stopped/deleted after inspection. This closes the F1 browser matrix; the later real-database entry records the three PostgreSQL rollback/replay/concurrency proofs passing.

### 2026-07-11: F1 Direct-Edit Visual Evidence Corrected

- Replaced the original disposable fixture's route ordering with a short-lived direct-edit fixture that handles `PATCH /records/{id}` before the generic read path. On the 1440px F1 Canvas, `客户阶段` changed from `新建` to `跟进中`, `续费关注` was removed from the tag list, and both Grid and Record Detail reread version `2`.
- The sanitized screenshot was saved and visually inspected at `project-docs/08-implementation/artifacts/stage07/f1-direct-edit-success-1440.png`; the final browser console contained no warning/error. The temporary fixture source/import and local port-4173 server were removed/stopped after the run.
- This improved the direct-edit browser evidence. The later same-day four-width matrix closes duplicate-name and pending-dialog browser evidence; delayed workspace/view replacement is correctly retained as an application scope-isolation proof. The PostgreSQL rollback/replay/concurrency evidence remains gated by the absent authorised disposable URL.

### 2026-07-10: F1 Independent Field Builder Initial Partial-Local Checkpoint

- F1 now has the documented endpoint `POST /tables/{table_id}/field-initializations`. The browser may submit only a display name, one of the eleven approved independent field types, `required` and validated choices for `status` / `single_select` / `multi_select`. It never receives or submits keys, policies, arbitrary options or raw saved-view configuration.
- The server projects `SafeTableField`, generates an opaque field key and serialized order, applies the default empty policy, appends only active explicit saved-view field lists, writes a sanitized audit event and uses an endpoint-scoped idempotency record in the same transaction. The Mini App clears protected table/view/form state, rereads the exact safe receipt and renders no optimistic column.
- The field drawer/sheet is accessible from the desktop toolbar, empty-table first-field state and the repaired nonempty mobile toolbar. Choice fields are usable immediately in record create and direct edit. The mobile repair is a CSS-only F1 consistency correction: the prior breakpoint rule hid every toolbar action after `筛选`, including `添加字段`; a red test captured the expected mobile visibility and now passes.
- Fresh four-width disposable Browser work covered 1440px/1280px desktop and 430px/390px mobile. It created `status` and `multi_select` fields, displayed the generated headers, created a record using the allowed choices, showed blank-name validation, 503 explicit retry, 409 lock and generic 403 denial, and confirmed zero final console warnings/errors. Workspace Ledger was reviewed beside the rendered 1440px F1 desktop state. All temporary fixture files were deleted and the local preview session was ended.
- Final automated verification reports `432 passed, 25 skipped` in the backend and `13` Mini App test files / `52` tests passed; the production build, `alembic heads` (`20260710_0021 (head)`) and offline `alembic upgrade head --sql` all passed. The skips include three F1 tests gated by the absent disposable PostgreSQL URL and are not acceptance evidence.
- At that checkpoint the evidence was deliberately not promoted beyond `partial-local`: the three executable real-PostgreSQL F1 cases were skipped without an authorised `STAGE06_LOCAL_DATABASE_URL`; and the original full fixture's generic read route ran before its test-only PATCH route, so the visual run did not capture direct-edit *success*. Component/application tests still covered the actual direct-edit path, and the fixture defect did not indicate a product runtime defect. Later direct-edit/browser and real PostgreSQL evidence supersedes this checkpoint.
- F2 relation/lookup, V1 additional views, imports/templates, governance, Package 4, real Telegram evidence and Stage07 acceptance remain out of scope or incomplete.

### 2026-07-10: F1 Independent Field Builder Design Approved

- The user approved F1 as the first safe schema-builder substage: independent fields `text`, `number`, `date`, `status`, `single_select`, `multi_select`, `user`, `checkbox`, `url`, `email` and `phone`; F2 retains relations/lookup and V1 retains additional views. The exact design is recorded in `docs/superpowers/specs/2026-07-10-stage07-f1-field-builder-design.md`.
- The design requires a new atomic, idempotent Mini App field-initialization endpoint, server-generated keys, default field policy, validated choice lists, table-row ordering serialization, server-owned safe saved-view visibility updates and a safe receipt/re-read flow. It explicitly forbids browser submission or receipt of raw policies, arbitrary options, raw view configuration or technical keys.
- The design also aligns the existing Canvas schema response with the Stage07 security contract by removing `permission_policy` and unrecognised options before they reach the browser. Choice-aware `multi_select` create/direct-edit support is part of F1 so a builder cannot create a required field that the app cannot use.
- This is a design/documentation checkpoint only. No F1 route, service, model, migration, client component or test has been changed yet; implementation-plan, TDD and browser evidence remain required.

### 2026-07-10: F1 Documentation and Test-First Plan Completed

- Added `STAGE_07_SUBSTAGE_F1_FIELD_BUILDER_PLAN.md` with ten ordered TDD tasks: cross-document contract alignment; safe Canvas schema projection; atomic domain/table-lock logic; strict idempotent endpoint; choice-value validation; panel/transport; protected App integration; record choice controls; real PostgreSQL proof; and final four-width visual evidence.
- Updated the active SDD, API/data-security contract, BDD, test plan, risk register, acceptance checklist, module index, requirement traceability audit and implementation/source indexes. They use one F1 endpoint/receipt vocabulary and explicitly preserve F2 relation/lookup and V1 additional-view exclusions.
- Task 1 documentation checks are complete. No runtime F1 file, dependency, migration or UI behaviour has changed by this checkpoint. The next plan task is a red test for the existing Canvas schema policy/raw-option leak.

### 2026-07-10: Original Stage07 Visual References Retained

- The user re-provided the three selected desktop reference images that were previously unavailable as repository assets. They are now retained as `Work Queue Atlas`, `Conversation Desk` and `Workspace Ledger` references under `project-docs/08-implementation/assets/stage07/`.
- Added `STAGE_07_VISUAL_REFERENCE_MANIFEST.md` to map each image to its owned product surface, state the shared light visual grammar and define later screenshot-comparison evidence. The references are visual-only: they neither import external product data nor expand the Stage07 API, authority, Bot or confirmation scope.
- This closes the missing-reference-asset gap noted by P3 documentation. It does **not** claim that the current Mini App matches all three references, nor does it replace the required 1440/1280/430/390 visual QA and real Telegram Mini App evidence.

### 2026-07-10: P3 Atomic Base/Table Builder Initial Real-PostgreSQL Evidence Gap

- The approved P3 design and task plan introduced only two atomic creation paths: `POST /workspaces/{workspace_id}/base-initializations` creates `Base + initial table + default Grid`; `POST /bases/{base_id}/table-initializations` creates `table + default Grid`. Each endpoint independently authorizes the required granular actions, performs resource/audit/idempotency writes in one transaction and returns only a safe receipt. The existing primitive creation endpoints remain compatible but are not used by the Mini App P3 flow.
- The database model/migration now carries `uq_views_one_default_per_table`, a PostgreSQL partial unique index for `is_default IS TRUE`. Domain and endpoint tests cover zero-field initialization, parent/resource audit behavior, validation/denial, replay/conflict, transaction behavior and the safe response boundary. At this checkpoint the three P3 real-PostgreSQL rollback/concurrency/default-index tests were skipped because `STAGE06_LOCAL_DATABASE_URL` was not configured; offline migration SQL confirmed one head `20260710_0021` and the expected additive partial-index statement. The later real database pass closes this bounded evidence gap without making a production or Stage07 claim.
- The Mini App Builder panel accepts names only, creates a fresh key per new panel, reuses one key only for explicit network/5xx retry, locks a `409` conflict until close, removes protected state on `401`/`403`, and never renders an optimistic Base/table. After a safe receipt, it refetches authorized Home/table/view lists and opens only the exact returned IDs. A fieldless new table states that Field Builder is the next substage and does not offer fake record/field mutation.
- Fresh browser QA used a disposable synthetic local fixture only. At actual `1280x720`, it verified Base `客户运营` with first table `客户` opens its exact default Grid with the zero-field message; an existing Base creates exact new table `待办` while its older table remains; blank-name validation remains inline; a simulated `503` keeps the panel/value for explicit retry and then opens `重试 Base`; and a simulated `403` removes the panel and renders the generic denied boundary with no optimistic `拒绝 Base`. At actual `390x844`, the labelled mobile creation sheet exposed Base/table inputs plus visible submit/cancel controls. The final feature console check returned zero warnings/errors.
- The fixture process was stopped and its source deleted after QA. The P3 evidence contains no production URL, user record value, raw policy/configuration, audit body or credential. The bounded slice does not complete Field/View Builder, import/template UX, governance, Package 4 or Stage07.

### 2026-07-10: Authorized In-Base Table Switch Is Implemented-Local

- Added an explicit Package 2 substage plan, `STAGE_07_SUBSTAGE_P2_TABLE_SWITCH_PLAN.md`, before code. It uses only the already-approved table/view/schema/presentation/record read contracts; builder, table creation, filters/sorts/groups, import/template, governance and Package 4 remain out of scope.
- `BaseCanvas` now renders only server-returned table summaries as accessible table tabs. Selecting a table chooses only that table's first server-returned saved view, then reads its schema, presentation and first authorized record window under the established protected-query keys.
- If no server-returned saved view belongs to the selected table, the UI enters the existing safe empty canvas without guessing a view ID or issuing schema/presentation/record reads. A table transition clears record-detail/create-drawer state and discards stale responses through the existing canvas/create-form generations.
- Tests cover tab selection, second-table authorized data replacement and the no-saved-view no-request boundary. Fresh checks: backend `407 passed, 19 skipped`; frontend `29 passed`; production build passed. A disposable desktop browser fixture confirmed Customers/All customers/Ada Co becomes Projects/All projects/Apollo, with the old cell absent. The fixture source/server were deleted/stopped.

### 2026-07-10: Server-Filtered Scalar Record Create Is Partial-Local

- Added the approved `GET /tables/{table_id}/create-form` contract to the Mini App and connected its only mutation path to existing `POST /tables/{table_id}/records`.
- The server filters writable first-slice scalar fields and removes raw options. Only validated `status` / `single_select` string choices may reach the browser; all other options are empty. If a required field is inaccessible or unsupported, `can_create: false` renders an unavailable state instead of an inevitably failing POST. This preserves the rule that browser schema visibility never becomes write authority.
- The Base view toolbar exposes the entry only through the application callback. The app loads the protected server model, discards stale workspace/Base/view responses, clears protected state on `401` / `403`, and clears all active-view cursor windows before it reads the authoritative first window after creation.
- Added backend options-redaction/unsupported-required regression coverage; frontend component, integration and renderer tests cover permitted form rendering, unavailable state, required validation, status choice submission, POST payload and view reload. Fresh full checks: backend `407 passed, 19 skipped`; frontend `26 passed`; production build passed.
- Disposable local browser fixture evidence covered desktop create-form rendering, required-field feedback, status select, successful record appearance after reload and the denied create boundary. Fixture server/code were stopped/deleted. The in-app browser did not apply a `390x844` viewport request (reported `1280x720`), therefore mobile create browser QA remains pending and is not counted as acceptance evidence.

### 2026-07-10: Package 1 Frontend Scaffold Verified

- Created the isolated `codex/stage07-mini-app-ui` worktree and preserved the Stage06 backend baseline (`400 passed, 19 skipped`).
- Added `mini-app/` React + Vite + TypeScript scaffold with Tailwind Vite integration, lucide-react, shadcn-compatible utility dependencies, Vitest and Testing Library.
- Added the first TDD smoke test for the `工作台` landmark. It was first observed failing because `App` did not exist, then passed after the minimal App/entry/build configuration was added.
- Verified `npm.cmd run test:run` and `npm.cmd run build` from `mini-app`.
- This checkpoint does not implement Workspace Home queues, Stage06 API transport, Base/table/view rendering, governance, Bot contacts, Mini App identity bootstrap or any proposed backend contract extension.

### 2026-07-10: Approved Read-Only Bootstrap And Home Slice Verified

- The user approved the minimal read-only API decision documented in `STAGE_07_API_DATA_SECURITY_CONTRACT.md`: `GET /mini-app/bootstrap` and `GET /workspaces/{workspace_id}/home`. No migration, Stage06 role change or new client-supplied permission claim was introduced.
- Added server-composed bootstrap data: only the verified identity's active memberships, active workspaces and server-derived navigation capabilities are returned. Inactive memberships and inaccessible workspaces are absent.
- Added a Workspace Home view model: active Base metadata and `pending_confirmation` draft summaries only. It deliberately excludes draft before/proposed field values, policies, creator identity and trace IDs.
- Added React API transport, App Shell, desktop sidebar, mobile navigation, Home queue, recent-Base rail, personal-assistant entry and workspace switching. The client renders only server-provided management capabilities and replaces the old Home before loading a switched workspace.
- TDD evidence: three backend contract/security tests first failed with missing routes and then passed; two frontend tests cover server-authorized visibility and workspace switch data replacement.
- Verification: `python -m pytest -q` in `backend` => `403 passed, 19 skipped`; `npm.cmd run test:run` => `2 passed`; `npm.cmd run build` passed.
- Rendered UI QA: the in-app Browser loaded the local contract fixture at desktop and `390x844` mobile viewport. Both displayed meaningful Home content with no Vite overlay or relevant console warnings/errors. Desktop and mobile workspace pickers were each exercised; selecting `项目中心` removed `客户管理` and rendered only `项目追踪`.
- Still not complete: browser screenshot comparison at desktop/mobile dimensions, protected query cache invalidation, Base/table/view canvas, record detail, templates/imports, governance, final draft review, Team Bot/personal assistant contract gate and live Telegram identity verification.

### 2026-07-10: Base Canvas Grid Slice Verified

- User approved the documented, read-only Base navigation extension. Added `GET /workspaces/{workspace_id}/bases`, `GET /bases/{base_id}/tables` and `GET /bases/{base_id}/views`; all reuse Stage06 membership/resource authorization and add no migration or write capability.
- Navigation summaries are deliberately narrower than primitive resources: Base lists exclude description/settings, and saved-view lists exclude `config` and `permission_policy`. Cross-workspace callers receive denial before any summary is returned.
- Home now opens a Base Canvas, loads authorized table/view navigation, then reuses existing schema and paginated view-record APIs to render the selected Grid with field labels and permission-filtered record values.
- Rendered UI QA: Home -> `客户管理` -> `客户表` -> `全部客户` produced the expected Grid at desktop and `390x844`; mobile retains a horizontally scrollable table instead of replacing it with cards. No relevant console warnings/errors were observed.
- Verification so far: 5 Stage07 backend API/security tests and 3 frontend interaction tests pass. Full backend regression and final Stage07 acceptance are still pending this slice's checkpoint.
- Not implemented in this slice: saved Kanban/Calendar/Form renderers. The safe navigation summary correctly excludes view presentation configuration, so choosing grouping/date/form fields in the browser would violate the approved contract. This requires a later separately approved, permission-filtered view-presentation contract.

### 2026-07-10: Field-Filtered View Presentation And Record Detail Slice Verified

- User approved the read-only View Presentation and Record Detail contract. Added `GET /views/{view_id}/presentation` and `GET /records/{record_id}` without schema, migration or write-permission changes.
- Corrected the pre-existing frontend contract leak in `GET /tables/{table_id}/schema`: route-level schemas now use the same field-read decision as view records, view presentation and record detail. Hidden field keys, metadata and values no longer reach a caller who cannot read them.
- Presentation returns normalized, permission-filtered field semantics only: visible field order; optional visible Kanban group field; optional visible Calendar date field; and Form field order. It never returns raw `config` or `permission_policy`.
- Base Canvas now permits switching saved views on the active table, renders Grid/Kanban/Calendar/Form according to normalized server semantics, and opens a field-filtered, versioned Record Detail panel from a record.
- Verification: 16 focused backend tests passed; 6 frontend interaction/render tests passed; browser QA confirmed desktop Grid -> Kanban -> Record Detail and `390x844` full-screen mobile Record Detail with no relevant console warnings/errors.
- Still incomplete: Form submission/editing workflow, filter/sort/group mutations, cursor pagination controls, conflict recovery, cache invalidation, imports/templates, governance and all Digital Employee/draft confirmation surfaces. Those must continue to use explicit authorized contracts and are not claimed by this slice.

### 2026-07-10: Version-Aware Direct Record Edit Slice Verified

- Reused the existing Stage06 `PATCH /records/{record_id}` request and its mandatory `expected_version`; no migration, Bot write path, schema contract or permission-model change was introduced.
- Record Detail now offers direct human edits for scalar field types already validated by Stage06 (`text`, status/select-like strings, `date`, `number` and `checkbox`). It submits only values changed by the user, so readable but unmodified fields are never accidentally included in a write request.
- The client passes the authoritative returned version/value back into the current detail and the rendered record window. The stored client response is reduced to keys present in the already server-filtered schema; it does not retain arbitrary fields returned by a primitive update route.
- A `409` version conflict is visibly distinct from a successful save. It remains a manual retry state in this slice; automatic authoritative reload and general protected-query invalidation are still pending.
- Complex `multi_select`, `linked_record`, `json` and `lookup` field values remain read-only in this direct-edit surface rather than being coerced into unsafe string writes. Their typed editor requires a separately documented interaction/data contract.
- Verification: initial focused tests were observed failing for the missing edit UI, all-field payload behavior and string-number serialization; after the implementation, 6 frontend interaction tests and the production Vite build passed. Full frontend regression subsequently passed (`9 passed`), and the full backend regression passed (`406 passed, 19 skipped` for unavailable historical online/local PostgreSQL environment variables).
- Browser QA used a disposable local contract fixture that was removed after the check. On desktop: Home -> Base -> record detail -> direct change of `Ada Co` / `12` to `Ada Labs` / `42` advanced the displayed and grid values to version `4`. At `390x844`, the latest versioned record opened in the full-width detail panel. No relevant browser console warnings or errors were observed; the temporary server and fixture were stopped/removed.

### 2026-07-10: Detailed Stage Documentation Package Requested

- User required Stage07 to follow prior-stage documentation depth, including SDD, BDD, contract, module index, test plan, risks and explicit component interactions.
- The approved design remains queue-first Home, table-first Base canvas, contextual Bot/draft surface, responsive desktop/mobile behavior and fail-closed UI safety.
- Contract extensions for workspace Bot contacts, knowledge sources, memory partitions and Mini App identity remain pending explicit approval.

### 2026-07-10: Requirement Traceability Audit Added

- Added `STAGE_07_REQUIREMENT_TRACEABILITY_AUDIT.md`, mapping each required Stage07 package, BDD flow, contract gate, safety requirement and acceptance evidence to actual current source and tests.
- The audit corrects stale planning wording in earlier design documents: a limited Package 1/2 vertical path exists, but it does not prove Package 2 completion and does not authorize Package 3/4 work.
- The audit explicitly records the remaining non-negotiable gates: protected-state/cache architecture decision; typed Form/create and builder interaction specifications; governance permission contract; field-filtered draft review contract; Package 4 Bot/knowledge/memory/Telegram decision; four-width visual QA; and approved real Telegram Mini App smoke.

### 2026-07-10: Authoritative Record Conflict Recovery Verified

- The Stage07 direct-edit path now treats `409` as stale local state: it rereads the authorized record detail and the active saved-view record window, then replaces the drawer/grid with the server-returned version before allowing another edit.
- `403` or `404` during this recovery enters the generic denied boundary instead of retaining the stale record surface. The refreshed record is filtered through the already field-filtered schema before it becomes client state.
- TDD evidence: an application integration test initially exposed a real mount-effect race that could close the editor immediately after an edit click. The synchronization effect now runs only after the record ID/version actually changes. The new application test proves one failed `PATCH`, a detail reread, a view-window reread and authoritative version `4` rendering.
- Verification: `npm.cmd run test:run` => `10 passed`; `npm.cmd run build` passed. Browser QA through a disposable local contract fixture produced `409`, reread `Ada Global`/version `4` into both Grid and Record Detail, displayed the retry notice and had no relevant console warning/error. The fixture/server were deleted/stopped after the run.

### 2026-07-10: Protected Query State Decision Proposal Prepared

- Prepared, but did not implement, `STAGE_07_TECHNICAL_DECISION_001_PROTECTED_QUERY_STATE.md` for user discussion. It recommends a memory-only `@tanstack/react-query` v5 boundary with verified user/workspace-prefixed keys, cancellation/removal on identity/workspace changes and no browser persistence.
- This is a technical selection gate. No dependency, cache migration, local persistence, API/schema/permission change or Package 4 behavior was added by the proposal.

### 2026-07-10: Server-Cursor View Pagination Verified

- Reused the existing Stage06 `next_cursor`/`has_more` view-record contract; no client filter, sort, group, hidden-field reconstruction or API/permission change was added.
- Base Canvas renders a load-more control only when the server returns a cursor. The next request sends that exact encoded cursor, appends only new record IDs and retains the previous authorized window if the next-page request fails so the user can retry.
- `403` during pagination transitions to the generic denied boundary; stale pages cannot overwrite a replaced view/workspace because each completion verifies the active view/cursor before updating local state.
- Verification: component and application tests cover server-cursor forwarding, deduplication and failure/retry presentation; focused frontend run passed `9 passed`; build passed. Browser QA with a disposable local contract fixture loaded page 2 (`Northstar`) at desktop and `390x844` mobile with no relevant console warning/error. The fixture/server were removed after the check.

### 2026-07-10: Protected Query State Decision Approved

- User approved Technical Decision 001. The next subphase may add the documented memory-only `@tanstack/react-query` v5 boundary after red tests prove user/workspace key isolation, request cancellation and 401/403/404 cache removal.
- The approval does not extend to browser persistence, backend API/schema/permission changes, governance, Bot/knowledge/memory or Telegram production verification.

### 2026-07-10: Protected Query Bootstrap/Home Slice Verified

- Installed the approved `@tanstack/react-query@5.101.2` without a persistence or Devtools plugin. `protectedQuery.ts` defines in-memory `QueryClient` defaults (`staleTime: 0`, `gcTime: 0`, no automatic retry), verified user/workspace-prefixed keys and scoped/all-Stage07 cancellation/removal helpers.
- Migrated Mini App bootstrap and Workspace Home only. Bootstrap receives a query cancellation signal; Home uses the verified identity/workspace key and forwards the signal to `fetch`. A workspace switch increments the request generation, cancels/removes the old workspace scope before starting the target Home request; 401 clears all Stage07 queries and 403 clears the affected workspace scope before a safe denied state.
- No browser storage, cache persister, backend endpoint/schema/permission change, governance screen, Bot feature or Telegram production flow was added. Base/table/view/record reads deliberately remain on the existing local state path until their own migration test slice.
- Verification: Query-key/scope-removal/default tests and API AbortSignal test passed; full frontend suite passed `17 passed`; production build passed. Browser QA switched from `运营中心` to `项目中心`, rendered only the new authorized Base and removed the old Base link with no relevant console warning/error. Static scan found no `localStorage`, `sessionStorage` or `persistQueryClient` usage.

### 2026-07-10: Protected Query Base-Open Slice Verified

- Migrated only the initial Base-open dependency tree to the approved memory-only query boundary: Base tables, Base views, the default table schema, default view presentation and the first cursor record window. All keys retain the verified `userId`/`workspaceId` prefix and each query forwards its cancellation signal to the existing transport; no backend/API/schema/permission change was made.
- A new red application test reproduced an actual stale-data risk: delayed Base tables/views from `workspace-1` could complete after a switch to `workspace-2` and restore the old Base canvas. The test now passes because the canvas request generation is invalidated on workspace switch and the old protected scope is cancelled/removed before the target Home loads.
- Verification: targeted App shell test passed `5 passed`; full frontend suite passed `18 passed`; `npm.cmd run build` and `git diff --check` passed. Browser QA used a disposable fixture to confirm `运营中心` Home -> `客户管理` Base -> `全部客户` Grid, including `Northstar / 进行中`, with no console warning/error. The test fixture and local server were removed/stopped after the check.
- Not yet migrated: direct-edit mutation refresh and conflict recovery. These remain deliberately scoped follow-up migrations, not implied by this Base-open slice.

### 2026-07-10: Protected Query Saved-View Slice Verified

- Migrated saved-view selection to the same verified user/workspace query-key contract. A different table receives its protected schema query; every selected view receives protected presentation and first-window record queries with forwarded cancellation signals.
- A red application test showed that delayed view presentation/records could restore `客户管理` after the user selected another workspace. The canvas request generation and workspace-scope removal now keep the old Base absent until the new workspace Home renders.
- Verification: targeted App shell tests passed `6 passed`; full frontend suite passed `19 passed`; production build and `git diff --check` passed. This slice has no backend/API/schema/permission change and does not imply record-detail, cursor continuation or mutation invalidation migration.

### 2026-07-10: Protected Query Record-Detail Slice Verified

- Migrated record-detail opening to an exact protected record key with the verified user/workspace scope and transport cancellation signal. A dedicated record-request generation prevents a slower earlier record response from replacing a later selection.
- A red application test proved the previous failure mode: delayed `record-1` detail could reopen the old workspace drawer after the user switched workspaces. The green implementation invalidates record requests on switch and also checks the active canvas generation before rendering a detail.
- Verification: targeted App shell tests passed `7 passed`; full frontend suite passed `20 passed`; production build and `git diff --check` passed. This slice does not alter the backend/API/schema/permission model and does not migrate cursor continuation or direct-edit/conflict mutation refresh.

### 2026-07-10: Protected Query Cursor-Continuation Slice Verified

- Migrated every load-more request to the approved verified user/workspace/view/cursor query key and forwarded the cancellation signal to the existing server-cursor transport. The opaque server cursor remains the only cursor source; no client filtering, sorting or cursor construction was added.
- The existing active workspace/view/cursor guard, record-ID deduplication and failure-retry surface remain authoritative. A new red/green integration assertion proves the cursor request carries the cancellation signal.
- Verification: targeted App shell tests passed `7 passed`; full frontend suite passed `20 passed`; production build and `git diff --check` passed. Mutation invalidation and conflict-refresh migration are deliberately still unimplemented.

### 2026-07-10: Protected Query Mutation Refresh Slice Verified

- Existing `PATCH /records/{id}` success now invalidates/removes the exact protected record and active-view first-window keys, then rereads both through the protected transport before replacing the UI. Existing `409` recovery uses the same authority path.
- TDD evidence: a success-save regression first failed because no record/view reread occurred. The green path proves an additional exact record read and updates both grid and detail to the authoritative value; no optimistic write, API, schema or permission change was added.
- Verification: targeted App shell tests passed `7 passed`; full frontend suite passed `20 passed`; production build and `git diff --check` passed.

## Next Step

P3, F1 and F2 are closed as bounded `implemented-local` substages; their real PostgreSQL and browser evidence is recorded above. V1's comprehensive design and dedicated TDD implementation plan are approved. V1-1 through V1-13 durability, strict schema, canonicalization, ACL/versioned mutation, server query, safe HTTP, local PostgreSQL evidence, typed Mini App transport, panels, App/BaseCanvas authoritative lifecycle and conflict/responsive recovery are complete as implementation/reconciliation work. Tasks 12--13 record a partial four-width Browser core matrix, actual role separation, bounded negative controls, safe relation candidate and conflict recovery; real-backend authority intersection and complete type-invalid/F2 Browser flows remain unaccepted. Import/template remains separately contract-gated. Governance and Package 4 remain separate contract gates.
