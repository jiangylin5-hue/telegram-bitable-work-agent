# Stage07 R1–R3 closure ledger

- Task 1: complete from prior session evidence; R0 contract inventory, customer-project regression fixtures and focused tests recorded. Pending branch-wide review only.
- Task 2: complete from prior session evidence; focused unit/PostgreSQL/Mini App/build checks recorded. Pending branch-wide review only.
- Task 3: completed; final identity/session/revocation (`31 passed`), queue-to-draft, cursor retry, editor and V1 invalid/F2/device evidence are reconciled without contract expansion.
- Task 4: completed; governance/draft/assistant/employee lifecycle and recovery UI observations are reconciled with focused backend/PostgreSQL/Mini App/build evidence and one bounded real-provider route smoke.
- Task 5: completed; truth corrections, selected visual matrix, temporary-fixture cleanup and final R0-R3 non-production baseline acceptance are recorded in `project-docs/08-implementation/evidence/stage07-r0-r3-final-reconciliation.md`.
- Live LLM evaluation Task 1: complete (uncommitted scoped diff; task review clean after fail-closed safety fix).
- Live LLM evaluation Task 2: complete (uncommitted scoped diff; independent review clean after the draft-payload fail-closed gate and focused regression evidence).
- Stage08 Package A Task 1: complete (uncommitted scoped diff; task review clean after budget/context-contract and canonical-state-machine fixes; 37 focused tests reported passing).
- Stage08 Package A Task 2: complete (uncommitted scoped diff; task review clean after JSONB-shape and canonical-state DB-constraint fixes; 3 protected local PostgreSQL tests reported passing, Alembic head `20260717_0028`).
- Stage08 Package A Task 3: complete (uncommitted scoped diff; task review clean after user-confirmed workspace row-lock concurrency repair, replay binding, and tracked-ticket fixes; 55 focused unit tests and 4 protected local PostgreSQL tests reported passing, including direct `pg_blocking_pids` lock evidence).
- Stage08 Package A Task 4: complete (uncommitted scoped diff; two independent review rounds clean after canonical-ticket, current-role permission and ticket/draft transition-lock repairs; targeted unit regressions and protected local PostgreSQL `pg_blocking_pids` single-side-effect evidence recorded; no external send/provider/API action).
- Stage08 Package A Task 6: complete (uncommitted scoped diff; case-level child-process isolation, parent-side redacted DTO revalidation, bounded timeout cleanup and 1..2 batch concurrency are independently reviewed clean; 33 focused evaluator unit tests pass; no Provider/Telegram/notification/network action).
- Stage08 Package A Task 5: complete (uncommitted scoped diff; strict Runtime API, server-derived actor/ticket/state and a one-ticket sequential multi-invocation gateway are independently reviewed clean after strict nested-budget and validation-error redaction repairs; 86 focused API/gateway/runtime/contract tests pass; no Provider/Telegram/notification/network action).
- Stage08 Package B Task B1: complete (uncommitted scoped diff; versioned Memory/Candidate models, `20260718_0029` and UoW parity independently reviewed clean after evidence-only test repairs; 8 focused unit/local PostgreSQL tests pass with direct `FOR UPDATE` dual-session blocking evidence; no Provider/Telegram/network action).
- Stage08 Package B Task B2: complete (uncommitted scoped diff; typed safe projections and fail-closed lifecycle service independently reviewed clean after valid-state, finite JSON, timezone, expected-version, audit allowlist and workspace-row serialization repairs; 51 focused unit/local PostgreSQL tests pass with dual-session `pg_blocking_pids` same-fingerprint idempotency and conflict-version-chain evidence; no Provider/Telegram/network action).
- Stage08 Package B Task B3: complete (uncommitted scoped diff; confirmed-record reference-only outbox/materializer and internal HMAC identity token independently reviewed clean after two fix rounds; 81 focused B3/B2/runtime tests pass; processed replay, stale permission/source/field paths and zero/multi-rule policies fail closed; no PostgreSQL outbox concurrency evidence, Provider, Telegram, network or external action).
- Stage08 Package B Task B4: complete (uncommitted scoped diff; controlled group candidate/source adapter, fixed Decimal 0.85 gate, safe list/revoke APIs and local PostgreSQL evidence independently reviewed clean after two fix rounds; 101 B4 and 134 B3/B2/runtime focused tests pass; raw/transport carriers, generic group materialization bypass, fingerprint corruption, TTL/version ordering, lifecycle classification and autoflush visibility fail closed; no Telegram/Provider/network action).
- Stage08 Package B Task B5: complete (uncommitted scoped diff; real local PostgreSQL lifecycle/package closure independently reviewed PASS after a test-evidence fix round; confirmed-draft outbox concurrency uses a transition row lock before idempotency lookup, with direct `pg_blocking_pids` evidence that the second session blocks and later reuses the same reference-only event; Package B module suite 120 passed, Alembic head `20260718_0029`; no Provider/Telegram/network action). Package B is closed; this does not mark Stage08, Package C, real LLM evaluation, or deployment complete.
- Stage08 Package C2 Task 1: complete (uncommitted scoped documentation diff; independent review PASS with 0 Critical / 0 Important / 0 Minor; D1–D6, fixed long-window budgets, `best_effort_group_deletion`, opaque authority and C3/E ownership reconciled; stale-value scan and whitespace checks recorded; no code, migration, external call, or deployment action).
- Stage08 Package C2 Task 2: complete (uncommitted scoped diff; independent review PASS with 0 Critical / 0 Important / 0 Minor; controlled mapping/projection models, migration `20260719_0030`, UoW parity and 5 focused disposable local PostgreSQL tests passed; no ingress/API/network/Provider/Memory/RAG action. Default `DATABASE_URL` has a legacy isolated revision and remains a deployment preflight risk, isolated from Task 2 evidence).
- Stage08 Package C2 Task 3: complete (uncommitted scoped diff; independent review PASS after two minimal RED/GREEN repair rounds; verified new/edited local ingress writes only authorised group/supergroup projections, uses source identity without historical raw-body reads, preserves same-session rollback, edit replay idempotency and exception redaction; 38 scoped tests pass including 6 disposable local PostgreSQL and 5 existing webhook tests. No outgoing Telegram/API expansion/Provider/Memory/RAG action; default database revision risk remains isolated).
- Stage08 Package C2 Task 4: complete (uncommitted scoped diff; final independent review PASS with 0 Critical / 0 Important after four repair rounds; private authority/handle, fresh materialization, 120/60,000 window, count-only safe view, 30-day event retention, server-authorized/expiry purge and PostgreSQL narrow locks are implemented. User-confirmed `source_chat_type` provenance migration `20260720_0031` makes only verified group/supergroup rows readable; unknown/channel sources fail closed before text loading. Full Task 4 C2 regression 68 passed with real disposable PostgreSQL upgrade/downgrade and drift evidence; no API/network/Provider/Memory/RAG/C1/C3 action. Default database revision risk remains isolated.)
- Stage08 Package C2 Task 5: complete (uncommitted scoped diff; real disposable PostgreSQL lifecycle/drift/privacy/concurrency closure. A valid RED proved fresh materialization could otherwise read the old committed body during an uncommitted purge; the minimal PostgreSQL shared row lock now conflicts with the lifecycle writer and `pg_blocking_pids` proves the reader waits for the committed current outcome. Full integration file 20 passed; no C1/C3/API/network/Provider/Memory/RAG action. Default database revision risk remains isolated.)
- Stage08 Package C2 Task 6: complete (final independent re-review PASS with 0 Critical / 0 Important / 0 Minor after remediating the initial partial-zero-fragment and missing-`group` D6 category findings. Disposable PostgreSQL source head is `20260720_0031`; focused C2/C1 regression 151 passed, compile/static/diff gates passed. C2 is closed and C3 may start; Package C, Stage08, Provider evaluation and deployment remain incomplete.)
- Stage08 Package C3: design and task-level implementation plan recorded (C1/C2 private composition only; 36,000 content-level global cap, compression-pending handoff to Package E, no schema/API/permission/external side effect). Task 1 is complete; Task 2 onward and package acceptance remain pending.
- Stage08 Package C3 Task 1: complete (new strict safe-view contracts only; valid RED→GREEN and independent review PASS with 0 Critical / 0 Important / 0 Minor; 27 focused contract tests plus compile/static/diff checks passed. No C1/C2 public contract, database, external action, or persistence change.)
- Stage08 Package C3 Task 2: complete (private C1/C2 direct composer and renderer; real RED→GREEN plus final independent re-review PASS with 0 Critical / 0 Important / 0 Minor after closing stale mapping, cross-actor binding, forged zero-view lineage and true budget-branch coverage. 20 focused service and 178 C1/C2/C3 unit tests passed; no C1/C2 mutation, database, external action, persistence, provider or compression behavior.)
- Stage08 Package C3 Task 3: complete (private `group_compression_pending` handoff only; real `49 × 500 = 24,500` character C2 window RED→GREEN, same-user role lineage repair, and independent review PASS with 0 Critical / 0 Important / 0 Minor. Focused service 36 passed and C1/C2/C3 unit regression 194 passed; versioned-edit pending drift fails closed. No C1/C2 mutation, database, external action, persistence, provider, digest or compression behavior.)
- Stage08 Package C3 Task 4: complete (disposable PostgreSQL combined composition/re-read evidence; initial T0/T1 10-case RED/GREEN and later T2 split to 12 cases are explicitly documented, with fresh T3 `12 passed`, C3 unit `63 passed`, and C1/C2/C3 focused regression `211 passed`. Independent remediation re-review PASS with 0 Critical / 0 Important / 0 Minor; no production C3 code, C1/C2, schema, API, permission, external action or persistence change.)
- Stage08 Package C3 direct-to-pending remediation: task-level complete (Package C Task 5 initial review found an Important consumption-time transition defect: an old direct composite whose current C2 window became pending returned `""` and called the group materializer, discarding still-current C1 evidence. Real RED `2 failed`; minimal current-composite pending routing repair plus new regression passed targeted `2`, C3 service `38`, combined C1/C2/C3 unit `196`; independent remediation review PASS with 0 Critical / 0 Important / 0 Minor. Fresh Package C review remains required before closure.)
- Stage08 Package C: closed (fresh Task 5 package review PASS with 0 Critical / 0 Important / 1 non-blocking Minor after the retained direct-to-pending remediation record. Disposable PostgreSQL combined C1/C2/C3 final suite `213 passed`, strict C3 service `65 passed`, single Alembic head `20260720_0031`, compile/privacy scan/diff checks passed. The non-blocking Minor is the existing Starlette/httpx strict-warning environment issue; Package D, E/F, Provider evaluation and deployment remain separate.)
- Stage08 Package D Task D0: complete (dedicated `pgvector/pgvector:pg17` disposable container on loopback 55432; `vector=0.8.5`; unset `STAGE08_RAG_DATABASE_URL` explicitly skips and forged default/native URLs cannot fallback. Independent review PASS with 0 Critical / 0 Important / 0 Minor. Container deliberately remains running for D1; package closure owns `down --volumes`.)
- Stage08 Package D Task D1: complete (strict retrieval contracts, knowledge source/chunk ORM and migration `20260720_0032`; initial independent review found an Important missing source/workspace/version composite integrity invariant. Remediation added source unique triple plus exact chunk composite FK; fresh review PASS with 0 open findings. Dedicated pgvector final `62 passed -W error`; real cross-workspace/wrong-version attacks reject, downgrade/re-upgrade retains vector 0.8.5 and final head 0032. D2 may start.)
- Stage08 Package E E3-R1: complete (user-confirmed safe-execution adapter contract; sealed single-field/value intent plus factory-only hash safe context and write-time safe ticket/Gateway/Stage06 draft summaries. Initial review found safe/default replay provenance; minimal fail-closed repair and re-review closed it with 0 Critical / 0 Important. 90 focused unit tests and compileall passed; no PostgreSQL/Provider/Telegram/network/deployment action. R2/R3 atomic execution and graph integration remain pending.)
- Stage08 Package E E3: complete (R1 safe-execution adapter plus combined R2/R3 atomic materialization/current-state locks/hash-only replay/minimal audit projection are complete. User-confirmed strict AnalysisProvider unavailable -> `degraded` terminal cannot answer, cite, draft or dispatch; forged/shape-invalid/exception paths remain failed. Final independent review: 0 Critical / 0 Important / 0 Minor; 113 selected unit, 40 Stage06 default-regression, 2 real loopback pgvector PostgreSQL integration and compileall passed. No Provider, Telegram, deployment or external write.)
- Stage08 Package E: closed (E1–E5 final re-review 0 Critical / 0 Important / 0 Minor. E5 replaced no-op production fan-out with real C3/D4/general branches, isolated read-only child sessions with zero request-session worker touch, and internal cancel/deadline controls; E3 atomic draft/audit and E4 strict API/versioned safe replay remain intact. Compact E 218 passed, real loopback pgvector collaboration PG 3 passed, compileall/diff check passed. No real Provider, Telegram, deployment or external write. Package F is next.)
- Stage08 Package F F1: complete (opt-in OpenRouterStage08AnalysisProvider only; explicit httpx transport timeout is bounded by E5 remaining deadline/provider budget; output is strict and cannot construct draft values; raw/private data never persists. Independent review PASS 0 Critical / 0 Important / 0 Minor; 100 focused F1/E collaboration tests plus compile/diff passed. No network, OpenRouter, Telegram or deployment call.)
- Stage08 Package F F2: complete (fixed 12-case synthetic spawn evaluator after sequential independent-review remediation. Final child-local prompt guard validates all four real fixture marker categories case-insensitively before F1 transport; strict parent DTO records actual Provider invoked/completed/usage-presence only; fault/coordinator-only cases are explicit and F1-compatible. Final review PASS 0 Critical / 0 Important / 0 Minor; 39 focused offline tests. No env read, network, Telegram, webhook or deployment call. F3 remains a separately authorized bounded real Provider evidence task.)
- Stage08 Package F: complete for current non-production quality-evidence scope (F1 opt-in strict adapter, F2 12-case isolated evaluator and F3/R2/R3 versioned real Provider evidence). Final F4 review PASS 0 Critical / 0 Important / 0 Minor; R3 is one bounded synthetic batch 12/12, 0 timeout, Provider 9 invoked/9 completed/8 usage-presence, with strict action enum evidence. Original F3 11/12 HOLD and R2 evidence remain immutable. Milvus no-go recorded: pgvector stays baseline without production scale trigger. No Telegram, webhook, deployment, draft confirmation or external write; production readiness is separate.)
- Stage09 P1 Task 1: complete (uncommitted documentation-only diff; detailed parallel-isolation deployment contract independently reviewed PASS for spec compliance and task quality, 0 Critical / 0 Important / 0 Minor; fixed target revision `20260720_0032`, dry-run/LLM-off/fake/Provider-disabled constraints and the independent HTTPS-hostname external block are recorded. No remote write, deployment, migration, Caddy change, Telegram or Provider action.)
- Stage09 native-deployment decision: accepted by user on 2026-07-22. TDR-017 and `STAGE_09_NATIVE_SERVER_DEPLOYMENT_PLAN.md` now supersede the unimplemented Docker-specific P1 path; no Stage09 Docker bundle was created, and all future P1 tasks must use native systemd/PostgreSQL/Redis/Nginx assets. Historical Stage03 Docker/Caddy remains untouched.
- Stage09 Native P1-A N1: complete (uncommitted repository-only diff; runtime contract and offline fail-closed validators passed remediation re-review with 0 Critical / 0 Important / 1 Minor). The initial PostgreSQL query-host bypass was reproduced using a no-secret fixture, then closed by complete canonical PostgreSQL/Redis URL whitelists and a retained no-value-leak regression script; marker denial is now in the primary validator. The retained Minor is a superseded historical line in the N1 report's first verification summary; final native-package review must ensure it is clearly marked or removed. No server, Docker, database, Redis, systemd, Nginx, Telegram, Provider or network action.
- Stage09 Native P1-A N2: complete (uncommitted repository-only diff; native systemd/Nginx assets passed final remediation re-review with 0 Critical / 0 Important / 0 Minor). It distinguishes exact permitted historical Python code-compatibility entry names from prohibited Stage03 operational infrastructure, enforces unique non-optional security directives even with whitespace around `=`, and covers hostile unit/public Nginx input offline. `nginx -t` is explicitly SKIPPED because this workstation lacks Nginx; P0a/P1-B must obtain a real no-secret host result before deployment. No server, Docker, service, Nginx, database, Redis, Telegram, Provider or network action.
- Stage09 Native P1-A N3: complete (uncommitted repository-only diff; initial N3A was rejected for a bootstrap guard, Redis socket and least-privilege contract break. The focused remediation aligns the fixed Unix socket, adds psql unset/empty non-zero exits, moves Redis to `stage09-redis:stage09-redis-socket` without application runtime-env access, fixes the N1 Unix-socket regression, pins migration execution and reconciles documentation. Final independent review PASS with 0 Critical / 0 Important / 0 Minor. Windows Git Bash final aggregate fixture suite passed in about 34 seconds; psql/Redis/systemd live checks remain explicitly SKIPPED and P0a/P1-B-only. No server, Docker, database, Redis, service, Nginx, Telegram, Provider or network action.)
- Stage09 Native P1-A N4: complete (uncommitted repository-only diff; release layout, manifest and fixed-revision offline migration package passed two remediation reviews. Initial C-01/I-01 removed the inherited legacy Alembic URL fallback and replaced source-only tests with executing temporary fixtures; I-02 then expanded the sealed release to 21 required P1-B assets and made a missing API unit reject layout/manifest. Final R2 review: 0 Critical / 0 Important / 1 Minor; target-host execution remains intentionally unproven. P0a read-only evidence records native prerequisites, historical port occupancy and sudo capability; P1-B remote writes may now begin in the loopback-only phase.)
- Stage09 Native P1-B loopback deployment: complete (real server writes; native PostgreSQL 16 + pgvector, isolated `stage09_p1` database/role, native Unix-socket Redis and protected runtime were provisioned without touching Stage03 Docker, Nginx, Caddy, 80/443 or business data. Immutable `stage09-p1-20260722-r2` release layout/manifest and offline SQL passed; fixed migration `20260720_0032` applied. Redis/API/worker/outbox are enabled and active; Redis PONG and loopback `/health` returned success. Runtime retains dry-run Telegram, empty allowlist, LLM-off/fake workflow/provider-disabled. Public HTTPS remains blocked by residual docker-proxy port ownership and missing dedicated hostname. Detailed no-secret evidence: `project-docs/08-implementation/evidence/stage09-p1-native-loopback-deployment.md`.)
